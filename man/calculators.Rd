% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculators.R
\name{calc_res}
\alias{calc_res}
\alias{ev_rest}
\alias{plot_res}
\title{Calculate Resources}
\usage{
calc_res(raster_grade, raster_thickness, area, density = 1)

ev_rest(res_table, actual_production)

plot_res(
  tonnage_raster,
  area,
  title = "Resource Estimation",
  subtitle = "",
  col_palette = "Spectral"
)
}
\arguments{
\item{raster_grade}{A SpatRaster containing the estimated grade values.}

\item{raster_thickness}{A SpatRaster containing the estimated thickness values.}

\item{area}{An sf polygon outlining the resource boundary.}

\item{density}{Numeric, the bulk density or specific gravity of the material (default = 1.0).}

\item{res_table}{A data.frame or tibble, typically the \code{table} output from \code{calc_res()}.}

\item{actual_production}{Numeric, the actual observed production values.}

\item{tonnage_raster}{A SpatRaster representing the calculated metal content or resources per grid cell.}

\item{title}{Character, main title for the plot.}

\item{subtitle}{Character, subtitle or panel label content (e.g., total calculated resources).}

\item{col_palette}{Character, color palette for the raster mapping. Default is "Spectral".}
}
\value{
A list containing the \code{raster} of calculated metal content (tonnage)
and a \code{table} summarizing the area, expected volume, average grade, and total metal content per polygon.

The original resource table with appended columns for actual_production and recovery_factor.

A tmap plot object suitable for viewing or export.
}
\description{
Calculates the volume, grade, and metal content (tonnage) for a resource block.
This calculation is generalized for any commodity, utilizing geometric volume
and grade block models.

Compares the calculated resource model against actual production (realization) values
to generate a recovery factor or "Koefisien Hasil".

Uses tmap to generate standardized spatial mapping plots, such as tonnage distribution over a block.
}
